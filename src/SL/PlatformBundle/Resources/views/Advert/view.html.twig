{% extends "SLPlatformBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}
{% block body %}
    <h1>{{ advert.title }}</h1>
    <div>
        <p>Par {{ advert.author }} le {{ advert.date|date("m/d/Y") }}</p>
        <p> {{ advert.content }} </p>
    </div>
{% endblock %}